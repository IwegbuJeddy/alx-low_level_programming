#include <stdio.h>
/* Betty docs */
int main(void)
{
int num;
char let;
do {
putchar(num);
++num;
} while (num < 10);
do {
putchar (let);
} while (let > 'a' && let <= 'f');
putchar('\n');
return (0);
}
