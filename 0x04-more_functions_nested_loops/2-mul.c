#include "main.h"
/**
* mul - Multiplies two integers
* @a : integer to be multiplied with b
* @b : integer to be multiplied with a
* Return: returns the value of c, the product
*/
int mul(int a, int b)
{
return (a * b);
}
